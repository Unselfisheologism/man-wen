# This is a placeholder keystore.properties file.
# Remove or replace this file with actual keystore credentials for production builds.
# The file should contain:
# storeFile=<path-to-keystore>
# storePassword=<keystore-password>
# keyAlias=<key-alias>
# keyPassword=<key-password>

# For GitHub Actions builds where keystore is not needed:
# You can leave this file empty or remove it entirely.
# The build will skip signing when keystore.properties is not found.

# Uncomment and add your keystore properties when building locally:
# storeFile=../keystore.jks
# storePassword=your_store_password
# keyAlias=your_key_alias
# keyPassword=your_key_password